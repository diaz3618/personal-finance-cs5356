# Copy this file to .env and fill in your local MySQL credentials.
# .env is gitignored so each teammate keeps their own.

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your-mysql-password
DB_NAME=finance_tracker
