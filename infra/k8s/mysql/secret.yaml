# SECURITY: Replace placeholder values with real credentials before deploying to
# a real cluster. Use sealed-secrets (https://github.com/bitnami-labs/sealed-secrets)
# or an external secrets operator. Never commit real credentials to git.
apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: pf-tracker
type: Opaque
data:
  MYSQL_ROOT_PASSWORD: Y2hhbmdlbWU=
  MYSQL_PASSWORD: Y2hhbmdlbWU=
